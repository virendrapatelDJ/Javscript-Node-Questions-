const response = require('./api-response');

const users = response.users;
console.log(users);

// 1. filter users where age is less then 30

// 2. add canVote field inside all user objects where age > 18

// 3. calculate Average height of users;
// Hint : avgHeight = sum of heights / number of users
